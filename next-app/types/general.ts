export type TMenuItem = {
    trigger: string, 
    color?: string
};

export type TLoadersList = {
    type: string,
    name: string
};

export type TGlobalSize = {
    x: number,
    y: number
};